/*Question
Given an integer array arr, find the contiguous subarray (containing at least one number) which has the largest sum and return its sum.

A subarray is a contiguous part of an array.
*/


class Solution {
    public int maxSubArray(int[] arr) {
        
        
        // Your code here
        int sum=arr[0];
        // sum to find max sum of subarray
        int dp[]=new int[arr.length];
        //An optimized array
        dp[0]=arr[0];
        //starting with first element
        for(int i=1;i<arr.length;i++)
        {
        //here finding maximum positive from previous element,if negative then choose 0 and then add current element
        //Likewise fill whole array
            dp[i]=max(dp[i-1],0)+arr[i];
        }
        for(int i=0;i<arr.length;i++)
        {
        //Here finding max in the array that we fill previously
        sum=max(dp[i],sum);
        
        }
      //  System.out.print(sum);
        return sum;
    }
    //Method to find max of two elements
   public static int max(int a,int b)
    {
        if(a>b)
        {
            return a;
        }
        else
        {
            return b;
        }
    }
}
